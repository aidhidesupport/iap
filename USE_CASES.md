# 完成条件・途中確認・再開を、一つの例で試す

IAPは、説明・確認・作業へ戻る負担を、本人と道具や周囲で分担するための仕様草案です。ここでは、架空のCSV集計を三つの場面に分けます。文章の例はそのままコピーして、自分が扱える小さな架空例に置き換えられます。文章を貼るだけで、継続的な監視や自動照合が有効になるわけではありません。

## 1. 何をもって完成か、依頼の時に決める

```text
目的：金額1列のCSVを集計する。
渡すもの：実行結果と、条件ごとの確認報告。
完成条件：
- 見出しなしの100・200・300を合計すると600になる。
- 100・abc・300では2行目のエラーを示し、合計を確定しない。
今回扱わないもの：ログイン、DB、複数列、小数、引用符。
判断を戻す場面：目的、完成条件、今回の範囲を変える必要が出たとき。
```

PMが依頼の目的と条件を伝え、別の人が担当する開発者とAIが作業します。依頼が曖昧なら、依頼元に不足を確認します。開発者が推測した条件を、PMの承諾済みにしません。

自分で架空例を読む段階では、二人の参加は不要です。実装を使った二人の協働を検証したことにはなりません。

## 2. 途中成果を見て、次の一手を決める

正常入力だけを実行したなら、次のように整理できます。これは説明用に作った途中報告です。

```text
できたこと：100・200・300で600になることを確認した。
残っていること：文字を含む入力は未確認。
次の一手：100・abc・300を実行し、行番号と合計の扱いを記録する。
判断が必要なこと：現時点ではなし。
```

通常の未完成は作業途中です。合意した範囲で修正・確認できるなら、その作業を続けます。途中でログイン機能を作り始めた場合は、完成条件との関係を確かめ、範囲変更ならPMへ切り出します。

完成条件を書いた文章と、実際の成果を確かめる処理は別です。確認の起動を本人の思い出しだけに任せないため、実装では作業の節目に照合を組み込みます。起動できなかった時は、その状態も示す必要があります。

## 3. 中断後、古い確認を引き継がず戻る

```text
前回の目的：金額1列のCSV集計を、正常・不正入力で確かめる。
前回までの確認：正常入力は確認。文字を含む入力は未確認。
再開する最初の作業：不正入力を実行して、結果を報告に残す。
再開前に見ること：依頼の条件と成果が、前回から変わっていないか。
```

成果や条件が変わっていたら、前回の評価を現在の確認済みとして使いません。IAPの基本実装0.2.5では、指定成果や契約の変更で以前の評価が失効します。変更後の正しさを自動的に保証する機能ではありません。

## 配布物で変化を確認する

macOS/LinuxとNode.js 22.13以降で、[開発者ガイド](CODEX_GUIDE.md#入手して確かめる)から0.2.5を入手・展開します。同梱の架空例は、PMの参加やCodexへのログインなしで試せます。以下は展開した`iap-codex-0.2.5`フォルダーで、同じターミナルから順に実行します。

```sh
iap_example=$(mktemp -d)
cp -R example/. "$iap_example/"
node checkpoint.mjs verify "$iap_example"
node checkpoint.mjs share "$iap_example"
```

`valid: true, ready: false`、共有文の「開発者側で修正継続」が想定結果です。同梱例はCSVの検証結果がなく、範囲外の設計が進んでいる例なので、条件達成の扱いにはなりません。

```sh
printf '\nお試しの追記。実際のCSV検証結果ではありません。\n' >> "$iap_example/artifact.txt"
node checkpoint.mjs share "$iap_example"
node checkpoint.mjs handoff "$iap_example"
```

共有文が「最新成果は未評価」、再開情報が`assessmentCurrent: false`になります。古い評価を流用しないことを確認できます。この操作は新しい評価を作りません。

IAPの照合を休止・再開する操作も、同じ一時フォルダーで確認できます。

```sh
node checkpoint.mjs pause "$iap_example"
node checkpoint.mjs resume "$iap_example"
```

順に`paused: true`、`paused: false`が返ります。IAPの休止は、Codex本体や作業中のプログラムを停止する操作とは別です。この手順は案件にフックをインストールしません。一時フォルダーの架空記録は残ります。

2026-09-23に、公開配布物0.2.5で上記の状態変化と再開情報を確認しました。同一Mac上でのコマンド確認であり、Codexセッションのフック動作・第三者の導入・利用効果の検証ではありません。

[ひとりで試すためのガイド](https://aidhidesupport.github.io/iap/guide/developer/#before) / [短い感想を残す](https://github.com/aidhidesupport/iap/issues/new?template=experience.md) / [検証の現在地](STATUS.md)
